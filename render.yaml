services:
  - type: web
    name: restaurant-waitlist
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: ADMIN_PIN
        sync: false
